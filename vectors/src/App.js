import "./App.css"
import US2 from "./US2.js"
import CentralPanel from "./CentralPanel.js"

const instruments = [<US2 />,]
// const instruments = [<CentralPanel/>]

export default function App() {
    return instruments.map(i => (i))
}